type User {
  id: ID! @id
  username: String! @unique
  password: String!
}

type Supplier{
  id: ID! @id
  name: String! @unique
  email:String
  phone: String
  address: String
  amounttotake: Float @default(value:0)
  amounttopay: Float @default(value:0)
  paid: [PaidDetail!]!
}

type PaidDetail{
  id: ID! @id
  createdAt : DateTime @createdAt
  description : String
  amountpaid : Float
  amounttaken:Float
  supplier:Supplier!
}

type LineItem{
  id: ID! @id
  name: String! @unique
}

type Category{
  id: ID! @id
  name: String!
  lineitem:LineItem!
}

type Product{
  id: ID! @id
  barcode: String! @unique
  name: String!
  sellPrice: Float
  wholesalePrice: Float
  noofpieces: Int @default(value:0)
  category:Category!
  stock:[StockItem!]!
}

type StockItem{
  id: ID! @id
  createdAt: DateTime! @createdAt
  badgeNumber: String
  noofpieces: Int!
  buyPrice: Float!
  sellPrice: Float!
  wholesalePrice: Float!
  expiry: DateTime
  product: Product!
  supplier:Supplier!
}

type SalesItem{
  id: ID! @id
  createdAt:DateTime! @createdAt
  noofpieces:Int!
  buyPrice:Float
  sellPrice:Float
  wholesalePrice:Float
  profit:Float
  product:Product!
  supplier:Supplier
  salesTicket:SalesTicket!
}

type SalesTicket{
  id: ID! @id
  createdAt:DateTime! @createdAt
  receipt:String! @unique
  sales:[SalesItem!]!
}

type Expense{
  id: ID! @id
  createdAt: DateTime! @createdAt
  description: String!
  amount: Float!
}

type Notification{
  id: ID! @id
  type: String!
  info: String!
}

type Barcode{
  id: ID! @id
  code: String! @unique
  name: String!
}